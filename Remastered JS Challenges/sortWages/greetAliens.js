const greetAliens = arr => {
    for (let i = 0; i < arr.length; i++){
        console.log('Oh Glourious ' + arr[i] + ' ' + 'we welcome you to planet earth')
    }
}

const aliens = [ "Daybreakians", "Vulcans", "Dothraki"]

greetAliens(aliens);